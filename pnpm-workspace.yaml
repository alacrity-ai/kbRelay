packages:
  - 'apps/*'
  - 'packages/*'

# pnpm 10 blocks dependency build scripts by default. Allow the ones our
# stack genuinely needs to compile/fetch binaries at install time.
onlyBuiltDependencies:
  - esbuild
  - better-sqlite3
  - workerd
