# Flat node_modules (npm-like) for maximum compatibility with wrangler,
# vite, esbuild's platform binaries, and better-sqlite3's native build.
# pnpm still installs fast via its hard-linked global store.
node-linker=hoisted
